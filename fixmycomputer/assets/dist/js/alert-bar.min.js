"use strict";

/**
 * @author		Brett Shenk (https://www.linkedin.com/in/brett-shenk-59480794/)
 * @copyright	Fix My Computer  2021
 * 
 * @package  	Alert Bar Functions
 * @version		1.0
 * 
 * @property {string}    global_cookie_status
 * @property {number}    global_cookie_time
**/var $;

/**
 * Cookie functions
 * 
 * setCookie - To create a cookie through javascript.
 * getCookie - Get the cookie you just made.
 * 
 * @param {string} 	name 
 * @param {*} 		value 
 * @param {number} 	days 
**/
function setCookie(e, t, r) {
    var o, n = "";
    r && ((o = new Date()).setTime(o.getTime() + 24 * r * 60 * 60 * 1e3), n = "; expires=" + o.toUTCString()), 
    document.cookie = e + "=" + (t || "") + n + "; path=/; Secure";
}

/**
 * @param {string} 	name 
 * @returns The value of the cookie
**/function getCookie(e) {
    for (var t = e + "=", r = document.cookie.split(";"), o = 0; o < r.length; o++) {
        for (var n = r[o]; " " == n.charAt(0); ) n = n.substring(1, n.length);
        if (0 == n.indexOf(t)) return n.substring(t.length, n.length);
    }
    return null;
}

void 0 === $ && ($ = jQuery), document.addEventListener("DOMContentLoaded", function(e) {
    var t = document.querySelector(".site-header").offsetHeight + "px";
 // If the alert bar is enabled along with cookies
    "enabled" == global_cookie_status ? ("closed" != getCookie("alert-bar") && setTimeout(() => {
        document.querySelector(".site-header").classList.add("enabled"), document.querySelector("#nav-placeholder").style.height = t;
    }, 500), document.querySelector('.alert-bar-container button[type="button"]').addEventListener("click", function() {
        setCookie("alert-bar", "closed", global_cookie_time), document.querySelector(".site-header").classList.remove("enabled"), 
        document.querySelector("#nav-placeholder").style.height = "129px";
        var e = document.querySelector(".alert-bar-container").offsetHeight + "px";
        document.querySelector(".site-header").style.transform = "translateY(-" + e + ")";
    })) : (setTimeout(() => {
        document.querySelector(".site-header").classList.add("enabled"), document.querySelector("#nav-placeholder").style.height = t;
    }, 500), document.querySelector('.alert-bar-container button[type="button"]').addEventListener("click", function() {
        document.querySelector(".site-header").classList.remove("enabled"), document.querySelector("#nav-placeholder").style.height = "129px";
        var e = document.querySelector(".alert-bar-container").offsetHeight + "px";
        document.querySelector(".site-header").style.transform = "translateY(-" + e + ")";
    }));
});
//# sourceMappingURL=alert-bar.min.js.map
